package dmc;

import java.util.Vector;

public class Test {
	 /*private static int numOfAcceptances = 0;
	public static Vector acceptOrReject(Vector oldState, Vector newState) {
    	numOfAcceptances++;
    	return newState;
    }
	
	
	public static void main(String[] args) {
		
		Vector walkers1 = new Vector(5);
		Vector walkers2 = new Vector(5);
		int counter = 0;
		for(int i = 0; i < 5; i++) {
			walkers1.add(new Walker(++counter));
		}
		
		for(int i = 0; i < 5; i++) {
			walkers2.add(new Walker(++counter));
		}
		
		Vector walkers3 = new Vector(); 
		walkers3 = acceptOrReject(walkers1, walkers2);
		
		for(Object k : walkers3) {
			System.out.println(((Walker) k).x);
		}
	}	*/
}
